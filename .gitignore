# Sensitive rendered configs (never commit these)
config/opencode/opencode.rendered.json
config/git/.gitconfig.rendered
*.rendered.*

# Build artifact
dotfiles

# OS
.DS_Store

# Logs and temp files
.log
*.log

# Plan docs (not configs)
docs/superpowers/

# Laravel Herd runtime state (contains local paths like lastSite)
config/herd/herd.json
