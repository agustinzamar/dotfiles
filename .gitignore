# Raycast exports: encrypted, but this repo is public and the export bundles
# every extension preference. Keep them in iCloud instead.
*.rayconfig

# OS
.DS_Store

# Logs and temp files
.log
*.log

# Backup files
*.bak
*.bak.*

# SDD task artifacts
.superpowers/
docs/superpowers/
# Local Pi runtime state
.atl/
.pi/
.omo/

# Local build artifacts (dot-tui binary + bun build output, no source yet)
bin/dot-tui
tools/tui/
