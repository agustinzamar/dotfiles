root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2
switch_case_indent = true

# shfmt reads this file, so it keeps the repo's existing 2-space shell style
# instead of reformatting everything to tabs.
[Makefile]
indent_style = tab

[*.md]
trim_trailing_whitespace = false
